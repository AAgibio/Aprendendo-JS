// Function declaration -> tem seus usos
function soma(x, y) {
    return x, y
}

// Function expression -> mais usada
const sub = function (x, y) {
    return x - y
}

// Named function expression -> menos usada
const mult = function mult(x, y) {
    return x * y 
}